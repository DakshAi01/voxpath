{
  "name": "voxpath-monorepo",
  "version": "1.0.0",
  "description": "VoxPath MCP-backed chat workspace",
  "scripts": {
    "dev": "npx concurrently --kill-others \"npm run dev --prefix frontend\" \"cd backend && .venv\\Scripts\\python.exe main.py\""
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
