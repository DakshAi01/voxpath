# VoxPath Frontend

This frontend is a Next.js chat UI for the current VoxPath product.

## Current scope

- Text-only chat
- Backend health polling
- MCP-backed backend responses exposed through `/chat`
- Landing page copy aligned to the live product

## Run locally

```bash
npm run dev
```

The frontend expects the backend API at `http://127.0.0.1:8000`.
